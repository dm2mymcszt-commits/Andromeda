#import <Foundation/Foundation.h>

// Discovers existing containers only. It never creates, modifies or deletes one.
FOUNDATION_EXPORT NSDictionary<NSString *, id> * _Nonnull TRLegacyLocations(void);
